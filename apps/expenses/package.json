{
  "name": "@shaikron/expenses",
  "version": "0.1.0",
  "private": true,
  "description": "Shaikron — Expense Tracker product",
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: configure dev server'",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "@shaikron/shared": "workspace:*",
    "@boilerplate/database": "workspace:*",
    "@boilerplate/auth": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
